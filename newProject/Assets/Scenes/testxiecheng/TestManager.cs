using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        BroadcastMessage("Chiji");
        //SendMessage("Chiji");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
